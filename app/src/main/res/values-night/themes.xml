<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with dark mode support. -->
    <style name="Theme.PhotoStudio" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize your night theme here -->
        <!-- Background color for the night mode -->
        <item name="colorSurface">@color/black_1</item>

        <!-- Accent color for the night mode -->
        <item name="colorAccent">@color/teal_700</item>

        <!-- Error color for night mode -->
        <item name="colorError">@color/fireBrick</item>

        <!-- Text color on surfaces -->
        <item name="colorOnBackground">@color/white</item>
        <item name="colorOnSurface">@color/white</item>

        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/black</item>
    </style>
</resources>
